/** @type {import("prettier").Config} */
const config = {
    trailingComma: 'all',
    printWidth: 120,
    tabWidth: 4,
    semi: true,
    singleQuote: true,
    arrowParens: 'always',
    bracketSpacing: true,
};

export default config;
